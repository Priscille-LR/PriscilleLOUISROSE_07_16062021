{
  "name": "starter-kit",
  "version": "1.0.0",
  "description": "",
  "scripts": {
    "parcel": "parcel index.html",
    "start": "parcel index.html",
    "dev": "parcel index.html",
    "prebuild": "npx rimraf build",
    "build": "parcel build index.html --no-minify  --experimental-scope-hoisting && cp _redirects dist/_redirects",
    "deploy": "gh-pages -d dist",
    "lint": "eslint ./scripts/**/*.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "devDependencies": {
    "@types/bootstrap": "^5.0.16",
    "@types/core-js": "^2.5.4",
    "parcel-bundler": "1.12.5",
    "sass": "^1.34.1",
    "typescript": "^4.3.2"
  },
  "author": "Priscille",
  "license": "ISC",
  "dependencies": {
    "autoprefixer": "^10.2.6",
    "bootstrap": "^5.0.1",
    "postcss-cli": "^8.3.1"
  }
}
